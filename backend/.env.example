# Copy to .env for local runs (git-ignored). Never commit real secrets.
# Your Neon connection string (the pooled one, with -pooler, is fine for the app):
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx-pooler.REGION.aws.neon.tech/neondb?sslmode=require
# Local path to the private application ledger (only used by import_ledger.py):
LEDGER_PATH=/Users/lc/workspace/jd-based-resume/application_log.md
